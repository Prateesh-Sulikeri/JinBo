{
  "name": "jinbo-chatbot",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "predeploy": "node scripts/test-and-deploy.js",
    "deploy": "npm run predeploy && npm run start",
    "deploy:override": "node scripts/test-and-deploy.js --override && npm run start",
    "build": "npm run predeploy"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "fuse.js": "^7.1.0"
  },
  "devDependencies": {}
}
